# Normalize line endings
* text=auto eol=lf
*.sh text eol=lf
*.md text eol=lf
*.ts text eol=lf
*.json text eol=lf
*.svg text eol=lf

# Transport started as vendored code (see scripts/ATTRIBUTION.md) but is now
# substantially maestro-original (models/workflows/events + tests) — count it.
scripts/package-lock.json -diff

# Generated-ish assets collapse in PR diffs by default
docs/assets/*.svg -diff
